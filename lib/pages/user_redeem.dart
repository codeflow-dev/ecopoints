import 'package:flutter/material.dart';

class UserRedeemPage extends StatelessWidget {
  const UserRedeemPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Redeem items")),
      body: Column(),
    );
  }
}
